# Server-side weather configuration
# Set this only in Netlify's environment-variable settings.
# Never expose the weather key with a VITE_ prefix or commit a real value.
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here
