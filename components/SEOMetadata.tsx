import React from 'react';

export const SEOMetadata: React.FC<{ isSnowing?: boolean }> = ({ isSnowing }) => {
  // Placeholder metadata component to unblock the build.
  // Replace with actual react-helmet-async usage if needed.
  return null;
};
